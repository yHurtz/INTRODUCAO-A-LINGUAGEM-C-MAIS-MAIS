#include <iostream>
#include <cmath>

using namespace std;

int main () {
    float num;

    cout << "Digite um número inteiro:" << endl;
    cin << num;
        if (num % 2 == 0) {
            cout << num << "se trata de um número par." << endl;
            else if(num % 2 !=== 0)
        }
}